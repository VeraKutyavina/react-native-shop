import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 8,
  },
  button: {
    fontSize: 18,
    color: 'black',
    borderColor: 'black',
    borderWidth: 1,
    width: 24,
    display: 'flex',
    textAlign: 'center',
  },
  value: {
    marginHorizontal: 4,
  },
});
